{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "functions": {
    "api/lookup.js": {
      "maxDuration": 300,
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" }
  ]
}
